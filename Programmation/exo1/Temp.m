for i = 1:1000
    if mod(i,4) == 0
        disp('Execution en cours');
        pause(0.5);clc;
    end
    if mod(i,4) == 1
        disp('Execution en cours.');
        pause(0.5);clc;
    end
    if mod(i,4) == 2
        disp('Execution en cours..');
        pause(0.5);clc;
        end
    if mod(i,4) == 3
        disp('Execution en cours...');
        pause(0.5);clc;
    end
end